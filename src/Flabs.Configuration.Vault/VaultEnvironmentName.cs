

namespace Flabs.Configuration.VaultSharp.Extensions
{

    public static class VaultEnvironmentName
    {
        public const string VAULT_ADDR = nameof(VAULT_ADDR);
        public const string VAULT_TOKEN = nameof(VAULT_TOKEN);
    }
}
